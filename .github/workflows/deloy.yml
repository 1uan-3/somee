name: Deploy to Somee

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Somee via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: luan13.somee.com
          username: luanduyyy3131
          password: 3103Luanbdl8@
          local-dir: ./
          server-dir: /www.luan13.somee.com/
          dangerous-clean-slate: false
